// Constantes de configuración del sistema
// Centralizar aquí evita que el mismo número quede repetido en 4+ archivos

export const ADMIN_WHATSAPP_PHONE = "5359887863";

// URL pública de la app web (Vercel). El enlace del correo de recuperación de
// contraseña abre aquí. Si se deja vacía, se usa el "Site URL" configurado en
// Supabase; si se define, tiene prioridad — recomendado para que el enlace
// funcione también cuando el reset se solicita desde el APK (que no tiene barra
// de direcciones). Ej: "https://bisne-con-talla.vercel.app"
export const WEB_APP_URL = "";
